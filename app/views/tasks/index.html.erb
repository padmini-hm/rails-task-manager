<h1>All tasks</h1>
<ul>
    <% @tasks.each do |task| %>
        <%# <%= render 'tasks/task', task: task %>
        <li> <%= link_to task.title, task_path(task.id)%>
        <%= link_to 'Delete', task_path(task.id), data: {method: :delete, confirm: 'Are you sure?'} %>
        </li>
    <% end %>
</ul>

<%= link_to 'Create Task', new_task_path %>
